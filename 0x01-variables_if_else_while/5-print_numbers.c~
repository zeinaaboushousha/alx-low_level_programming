#include <stdio.h>
/**
 * main - print numbers of base 10
 * Return: Always (0)
 */
int main()
{
int base_ten = 0;
while (base_ten <= 10)
{
putchar(base_ten);
base_ten++;
}
putchar('\n');
return (0);
}
